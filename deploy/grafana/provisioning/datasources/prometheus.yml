apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Fixed uid so the provisioned dashboard can reference it.
    uid: streampulse-prom
    url: http://prometheus:9090
    isDefault: true
    editable: false
